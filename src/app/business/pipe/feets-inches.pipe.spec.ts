import { FeetsInchesPipe } from './feets-inches.pipe';

describe('FeetsInchesPipe', () => {
  it('create an instance', () => {
    const pipe = new FeetsInchesPipe();
    expect(pipe).toBeTruthy();
  });
});
